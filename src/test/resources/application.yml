auth:
  jwt:
    secret: asdf
spring:
  h2:
    console:
      enabled: true
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb
  jackson:
    property-naming-strategy: SNAKE_CASE
  jpa:
    generate-ddl: true
    show-sql: true
